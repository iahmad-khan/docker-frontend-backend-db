replicaCount: 1

image:
  repository: container-registry.registry.svc.local:3200/frontend
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 3001

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}


mongodb:
  auth:
    username: username
    password: password
    database: todos
  primary:
    persistence:
      enabled: true
      size: 8Gi
  service:
    port: 27017
