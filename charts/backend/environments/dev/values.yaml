replicaCount: 1

image:
  repository: localhost:32000/backend
  tag: v10
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3001

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}


mongodb:
  auth:
    username: username
    password: password
    database: todos
  primary:
    persistence:
      enabled: true
      size: 8Gi
  service:
    port: 27017
